{
  "name": "nordic-emobility-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:products": "node scripts/generate-products.mjs",
    "build": "npm run generate:products && echo Static site ready"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "stripe": "^14.0.0"
  }
}
